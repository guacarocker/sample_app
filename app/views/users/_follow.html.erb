<%= form_for(current_user.relationships.build(followed_id: @user.id), remote: true, id: "follow_form") do |f| %>

	<div><%= f.hidden_field :followed_id %></div>

	<%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>

